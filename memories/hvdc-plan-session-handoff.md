---
name: hvdc-plan-session-handoff
description: Handoff state for finishing the milestone-7-hvdc.md plan revision (remaining W6/W7/W8 edits + F1/F2 passes) in a fresh session
metadata:
  type: project
---

Handoff from a long Claude Code session revising `plans/milestone-7-hvdc.md` (HVDC / DC-line OPF feature). The session got long and the harness grew unstable (see [[harness-tool-issues]] item 6 — proxy reconnect/ghost outputs, one mid-turn API error). Pausing to hand off to a fresh session. **We are only revising the plan doc — NOT implementing code.** The user reviews maximally before any code is written; go section-by-section and pause for approval.

**Working method that held up all session:** edit with `Edit`/`Write`, then verify on disk with `Grep -n` (never trust the `Edit` echo). Use `sed -n 'A,Bp' <file>` for line-range reads (`Read` with offset is broken this session). Keep turns short. After any API error/ghost, re-verify on disk before retrying — an edit can land during an errored turn.

## What this revision has done
Two large reframes plus a critique backlog, all against `plans/milestone-7-hvdc.md`:
- **C7 box-model reframe (complete, committed):** the per-step box `[p_min_t, p_max_t]` is the *sole* internal representation; the four "modes" (scheduled/band/downward/free) are upstream `_hvdc_static_box` helpers producing the box; `scheduled` = degenerate box pinned by *coincident bounds* (no separate equality); loss branch chosen purely from the box's zero-crossing; two `df_hvdc_min`/`df_hvdc_max` `(T, n_hvdc)` frames.
- **Component-architecture reframe (in progress, W1-W6 done + W7 CLAUDE.md half done):** `hvdc.py` exposes named *methods* a constructor composes, NOT an omnibus tuple: `_hvdc_static_box` (numpy) -> `hvdc_injections(...)` (creates `p_in`/`p_out` `(n_hvdc,)` vars, returns already-scaled Convention-B addend via an `inv_baseMVA` `cp.Parameter`) -> `dc_operating_constraints(...)` (box bound + `p_out == coeff_vec * p_in` loss eq) -> `ac_operating_constraints(...)` (verbatim pass-through to the DC one) -> `hvdc_cost_expr(cost_coeffs, p_in)` (own cost, does NOT reuse `poly_cost_expr` — magnitude `|p_in|` vs signed `Pg`). Cost lives in `hvdc.py` (by-component). `hvdc.py` imports `cvxpy`+`numpy`, no other cvxopf module (corrected from the wrong "numpy only" claim). The `inv_baseMVA` `cp.Parameter` is late-binding (constructor sets `.value = 1.0/baseMVA`), NOT M13 fast-resolve; DNLP-compat with `cp.Parameter` was empirically confirmed (`solve(nlp=True, IPOPT)` honors `.value`).

## Remaining tasks (do in order, pause after each)
- **W6 — Step 7 doc bullet: DONE.** M16 staged as a planned CLAUDE.md/README addition inside the plan's Step 7 doc-update bullet (`plans/milestone-7-hvdc.md`).
- **W7 — CLAUDE.md + README.md (AUTHORIZED this session): CLAUDE.md DONE, README.md STILL TO DO.**
  - **CLAUDE.md done & verified on disk:** M16 table row at L512, M16 subsection at L585-623 (placement (a), right after the M15 subsection, before M8). User broadened the scope: M16 is NOT just dispatchable generators — it also covers **storage and nondispatchable**, all needing refactor into alignment. The subsection lists all three as a cross-cutting refactor. Do NOT re-insert these; they are already present. Do NOT rewrite the existing storage/nd "numpy only" import-chain lines (a closing note in the subsection already flags they describe current code and are intentionally left).
  - **README.md remaining — the ONLY unfinished W7 piece:** append ONE terse checkbox item to the end of the Roadmap list. The README roadmap is a `- [ ]`/`- [x]` checkbox list (terse phrases, no M-numbers, no bold, no colons) — NOT the CLAUDE.md prose style. Exact line to append (user-approved wording): `- [ ] Unify grid component model patterns (dispatchable generators, storage, nondispatchable → first-class composable components)`. It goes at the very end of the Roadmap list. NOTE: reads of the README tail were corrupted by ghost/buffered output at handoff time — re-`sed`/`wc -l` the file with clean output before appending so the offset is right; the list is near end-of-file (~L413) but re-confirm.
- **W8 — R2/R5 touch-ups:** at `plans/milestone-7-hvdc.md` **L590** (R2 body) convert `(1/baseMVA) * (Ch_from @ p_in + Ch_to @ p_out)` to the `inv_baseMVA`-parameter phrasing and note the `cp.Parameter` seam dissolves the old AC-vs-DC `cp.multiply` idiom-difference sign-bug risk R2 was about; **L631** (R5 body) update the `_make_hvdc_step_injections` reference to the new method names (`hvdc_injections`/`dc_operating_constraints`). (Line numbers as of handoff — re-grep, they drift.)
- **F1 — final holistic pass:** `git diff` the file vs last commit first to confirm no stray edits from the W5 API-error turn; then a global sweep for any surviving singular `df_hvdc` (should be `df_hvdc_min`/`df_hvdc_max`), any surviving `_make_hvdc_step_injections`, `(1/baseMVA)` literals that should be `inv_baseMVA`, and `_make_hvdc_step_injections`->`hvdc_injections` name drift; cover-to-cover read for seams between separately-edited sections; confirm "coincident bounds"/"degenerate (zero-width) box" terminology is uniform.
- **F2 — done as part of this handoff:** memory updated ([[harness-tool-issues]]); the 1-3 harness-orientation bullets the user asked for should be *printed to the user in chat* (not written to any file) — they wanted to place them in a harness preamble themselves.

## Key user decisions locked (don't relitigate)
- C2 depth = **plus** (done in W5): `hvdc_from_dcline` reverses highest-first `dclinecost` rows into lowest-first `cost_coeffs`, `n`-dependent padding (`n=2`->`(c0,c1,0.0)`, `n=3`->`(c0,c1,c2)`, reject `n>3`), plus a Gate 1 assertion on a synthetic row with distinct nonzero `c0!=c1!=c2`.
- dcline import = `mode="free"` optimizing over `[Pmin,Pmax]`; `Pf` is a non-binding reference (verified via MATPOWER manual §7.6: dispatchable dummy-generator pair). Gate 6b solves `formulation="ac"` vs the AC Pypower oracle.
- singlenode_dc silently ignores hvdc (accepted-and-dropped through the single positional dispatch site; see [[hvdc-silent-ignore-dispatch-constraint]]).
- Keep `downward` as a convenience box-builder. See [[hvdc-plan-mvp-scope]] and [[cvxpy-affine-equality-rule]].
